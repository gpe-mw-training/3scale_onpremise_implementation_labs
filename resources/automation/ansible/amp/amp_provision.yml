# Pre-reqs:
#   1)  Install the following as root:    pip install jmespath
#       - doing so allows for use of json_query task

- hosts: onprem-amp
  remote_user: root
  connection: local

  roles:
    - amp_provision 
    - 3scale_resources
